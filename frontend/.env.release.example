# Windows code signing (Electron Builder auto-detects these)
# Set CSC_LINK to a .pfx path or base64-encoded PFX content.
CSC_LINK=C:\path\to\codesign-cert.pfx
CSC_KEY_PASSWORD=your-pfx-password

# Optional Windows-specific overrides (if you use separate cert material)
# WIN_CSC_LINK=C:\path\to\windows-cert.pfx
# WIN_CSC_KEY_PASSWORD=your-pfx-password

# macOS signing/notarization (used only for mac builds)
# APPLE_ID=you@example.com
# APPLE_APP_SPECIFIC_PASSWORD=xxxx-xxxx-xxxx-xxxx
# APPLE_TEAM_ID=ABCDEFG123
